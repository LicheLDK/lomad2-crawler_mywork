# Lomad Crawler Web

```powershell
# 프로젝트 루트에서 API/Worker 기동 후
npm run web:dev
```

http://127.0.0.1:5173

Windows에서 Rollup native 오류 시:

```powershell
cd web
npm install @rollup/rollup-win32-x64-msvc --os=win32 --cpu=x64
```
