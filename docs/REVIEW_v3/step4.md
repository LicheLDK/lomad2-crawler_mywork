@작업지시서\_v3_P0.md @PROJECT_REVIEW_v3.md

이 지시서의 TASK 0-1 만 수행해줘.

선행 결정 (D1):

- (B) upsert 를 채택한다. 크롤 완료 시점(CrawlerService)의 자동 생성은 유지하고,
  Search Job 의 AI 매칭 결과가 기존 케이스를 갱신하도록 만든다.

가장 중요한 제약:

- 이 문제(리뷰 문서의 N1)는 코드 정적 분석으로 도출한 결론이고 런타임으로 증명되지
  않았어. 수정하기 전에 npm run infra:up 으로 인프라를 띄우고 검색을 1건 실행해서,
  생성된 investigation_cases 행의 점수 출처가 실제로 휴리스틱인지 먼저 확인해줘.
- 재현되지 않으면 수정하지 말고, 무엇을 관찰했는지 보고하고 멈춰줘.
- 사람이 상태를 바꾼 케이스(Open 이 아닌 케이스)를 AI 재평가로 덮어쓰지 않는 것이
  이 작업의 핵심 안전 조건이야. 반드시 spec 으로 고정해줘.
- P0 범위이므로 신규 마이그레이션은 만들지 않는다. 점수 출처 기록이 필요하면
  기존 metadata 또는 timeline 을 활용해줘.
- 직접 검색(POST /api/search) 경로에서도 케이스가 계속 생성되어야 한다.

로컬 커밋 1개로 끝내고, 완료 후 §4 보고 형식으로 보고해줘. push 는 하지 마.
