@작업지시서\_v3_P0.md @PROJECT_REVIEW_v3.md

이 지시서의 TASK 0-3, TASK 0-4 를 수행해줘.

선행 결정 (D3):

- (A) 레이블 정정을 채택한다. "오늘 Investigation" 타일은 "오늘 수집 결과"로
  레이블만 바꾸고, 조사 케이스 카운트 API 는 만들지 않는다.

규칙:

- TASK 1개 = 로컬 커밋 1개, 총 2개 커밋. push 는 하지 마.
- 백엔드는 변경하지 않는다. GET /api/ai/usage/today 는 이미 구현되어 있으니
  web/src/api.ts 에 노출만 하면 된다.
- 각 TASK 의 「변경 대상」 표에 없는 파일은 건드리지 마.
  필요하다고 판단되면 먼저 물어봐줘.
- TASK 0-4 에서 Rental·Analytics·System 의 기존 탭 활성 동작은 바꾸지 마.
  현재 페이지 fallback 과 이미 일치한다.
- 「완료 조건」을 모두 만족시키고 「검증」을 실행해줘.
- TASK 0-4 의 수동 확인 경로 8개도 확인해줘.

완료 후 §4 보고 형식으로 보고해줘.
