@작업지시서\_v3_P0.md @PROJECT_REVIEW_v3.md

이 지시서의 TASK 0-6, TASK 0-8, TASK 0-7 을 이 순서대로 수행해줘.

규칙:

- 지시서 §0 공통 규칙을 따른다. TASK 1개 = 로컬 커밋 1개, 총 3개 커밋. push 는 하지 마.
- 작업 시작 전에 §0 「환경 준비」의 baseline 3개를 먼저 실행해서 통과를 확인해줘.
  npm run build / npx jest --runInBand / npm --prefix web run build
  통과하지 않으면 작업하지 말고 보고해줘.
- 각 TASK 의 「완료 조건」 체크리스트를 모두 만족시키고 「검증」 명령을 실제로 실행해줘.
- TASK 0-8 은 파일 삭제 전에 지시서에 적힌 rg 재확인을 먼저 하고,
  참조가 하나라도 나오면 그 파일은 삭제하지 말고 보고해줘.
- TASK 0-8 의 「보류 항목」(web/src/api.ts 의 미사용 함수들)은 삭제하지 말고 유지해줘.
- TASK 0-6 에서 CI 에 lint 단계는 넣지 마. ESLint 는 TASK 0-5 에서 별도로 도입한다.
- 지시서 §2 「P0에서 다루지 않는 것」에 해당하는 작업은 절대 하지 마.
- 지시서에 없는 개선을 임의로 추가하지 마.

완료 후 §4 「보고 형식」 5개 항목으로 보고해줘.
